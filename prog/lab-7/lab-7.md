# Лабораторная работа №7. Деплой приложения на Flask с БД Redis

## Задание

Развернуть / запустить приложение, представленное в репозитории локально и создать отчет о его развертывание.
Развернуть / запустить приложение на play with docker (https://labs.play-with-docker.com/).
Развернуть / запустить приложение удаленно на машине, предоставленной преподавателем с учетом сетевых настроек и каталога, куда будет публиковаться приложение.  Учесть, что на этот сервер будет произведен деплой приложения студентов группы, т.е. надо менять порты, папку для деплоя и другие настройки приложения, которые должны быть отличны для разных студентов (чтобы не мешать друг другу).
Реализовать тестирование серверной логики и деплой Flask-приложения с использованием CI. 
Требования к бизнес-логике:

Счётчик не может быть меньше 0

При попытке уменьшить счётчик, если он равен 0:

сервер возвращает HTTP 400

в ответе JSON с сообщением об ошибке

Пример:
```
{
  "error": "Counter cannot be negative"
}
```
Тестирование (обязательная часть)
Необходимо реализовать минимум 3 теста с использованием pytest:

Тест инициализации

при первом запросе значение счётчика равно 0

Тест увеличения

после вызова /increment значение увеличивается на 1

Тест защиты от отрицательных значений

при попытке уменьшить счётчик ниже 0 сервер возвращает ошибку

📌 Redis можно:

либо запускать локально;

либо мокать через fakeredis (предпочтительно для скорости).

CI/CD в GitHub Actions workflow должен:

Устанавливаться при каждом push в main

Выполнять:

установку зависимостей

запуск pytest

Прерывать деплой, если хотя бы один тест не прошёл

Структура этапов:
```
checkout → setup python → install deps → run tests → deploy
```
⚠️ Реальный деплой: sync на сервер. 
```
backend/
 ├── app.py
 ├── requirements.txt
 ├── tests/
 │    └── test_counter.py
 └── redis_client.py
.github/
 └── workflows/
      └── deploy.yml
```
Представьте ссылку на свой репозиторий (форк репозитория) с демо и отчетом по выполнению работы.

## Решение:

Приложение: Flask (бэкенд) + Vue/Vite (фронтенд) + Redis (хранилище счётчика),
упаковано в Docker и оркестрируется через `docker compose`.

## 1. Архитектура и бизнес-логика

Сервис хранит одно значение `counter:value` в Redis и предоставляет REST API:

| Метод | Путь | Описание | Ответ |
|-------|------|----------|-------|
| GET  | `/api/counter`           | текущее значение           | `200 {"value": N}` |
| POST | `/api/counter/increment` | увеличить на 1             | `200 {"value": N}` |
| POST | `/api/counter/decrement` | уменьшить на 1             | `200 {"value": N}` или `400 {"error": ...}` |
| POST | `/api/counter/reset`     | сбросить в 0               | `200 {"value": 0}` |

Реализованные бизнес-правила:

1. **Счётчик не может быть меньше 0.**
2. При попытке уменьшить счётчик, равный 0:
   * сервер возвращает **HTTP 400**;
   * тело ответа — JSON `{"error": "Counter cannot be negative"}`.

Проверка:

![1.png](https://github.com/ZabivakaXD/Herzen_curse_3/blob/main/prog/lab-7/img/1.png)

![2.png](https://github.com/ZabivakaXD/Herzen_curse_3/blob/main/prog/lab-7/img/2.png)

## Тестирование и CI/CD

### Тесты (pytest + fakeredis)

Тесты лежат в `backend/tests/test_counter.py`. Redis не запускается
по-настоящему — используется `fakeredis` (in-memory), что делает прогон
быстрым и независимым от окружения. Покрыты обязательные сценарии:

1. **Инициализация** — при первом запросе `GET /api/counter` возвращает 0.
2. **Увеличение** — после `POST /api/counter/increment` значение растёт на 1.
3. **Защита от отрицательных значений** — `POST /api/counter/decrement` при
   нуле возвращает `400` и `{"error": "Counter cannot be negative"}`,
   а значение в Redis остаётся 0.

Тесты: 

![3.png](https://github.com/ZabivakaXD/Herzen_curse_3/blob/main/prog/lab-7/img/3.png)